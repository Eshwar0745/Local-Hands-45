# 🗺️ Live Tracking Architecture - Visual Overview

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CUSTOMER SIDE                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  📱 Customer Device                                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  CustomerTrackProvider.jsx                                    │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  🗺️  Leaflet Map                                       │  │   │
│  │  │  ┌──────────────────────────────────────────────────┐  │  │   │
│  │  │  │  📍 Provider Marker (Updates every 10s)          │  │  │   │
│  │  │  │  📌 Customer Location                            │  │  │   │
│  │  │  └──────────────────────────────────────────────────┘  │  │   │
│  │  │                                                         │  │   │
│  │  │  ⏰ Polling: GET /provider/track/:id                  │  │   │
│  │  │     Every 10 seconds                                   │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  📱 Nearby Providers Discovery                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  NearbyProvidersMap.jsx                                       │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  🗺️  Leaflet Map                                       │  │   │
│  │  │  ┌──────────────────────────────────────────────────┐  │  │   │
│  │  │  │  📍 Provider 1 (Live)                            │  │  │   │
│  │  │  │  📍 Provider 2 (Live)                            │  │  │   │
│  │  │  │  📍 Provider 3 (Live)                            │  │  │   │
│  │  │  │  📌 You are here                                 │  │  │   │
│  │  │  │  ⭕ 5km radius                                    │  │  │   │
│  │  │  └──────────────────────────────────────────────────┘  │  │   │
│  │  │                                                         │  │   │
│  │  │  🌍 Query: GET /provider/nearby                       │  │   │
│  │  │     ?lat=17.385&lng=78.486&radius=5000                │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘

                              ⬇️ API Calls ⬇️

┌─────────────────────────────────────────────────────────────────────┐
│                         BACKEND SERVER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  🔧 Express.js API Routes                                            │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  providerRoutes.js                                            │   │
│  │                                                                │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │  POST /provider/update-location                         │ │   │
│  │  │  ├─ requireAuth ✅                                       │ │   │
│  │  │  ├─ requireRole("provider") ✅                           │ │   │
│  │  │  └─ → updateLocation()                                  │ │   │
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  │                                                                │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │  GET /provider/track/:id                                │ │   │
│  │  │  └─ → getProviderLocation()                             │ │   │
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  │                                                                │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │  GET /provider/nearby                                   │ │   │
│  │  │  └─ → nearbyProviders()                                 │ │   │
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  │                                                                │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │  POST /provider/availability                            │ │   │
│  │  │  ├─ Check: onboardingStatus === "approved" ✅           │ │   │
│  │  │  ├─ Check: location.coordinates exists ✅               │ │   │
│  │  │  └─ → setAvailability()                                 │ │   │
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  🎮 Controllers                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  providerController.js                                        │   │
│  │                                                                │   │
│  │  📍 updateLocation(req, res)                                  │   │
│  │     ├─ Extract lat, lng from req.body                         │   │
│  │     ├─ Update User.location.coordinates = [lng, lat]          │   │
│  │     ├─ Update User.lastLocationUpdate = now                   │   │
│  │     └─ Return success                                         │   │
│  │                                                                │   │
│  │  🔍 getProviderLocation(req, res)                             │   │
│  │     ├─ Find User by req.params.id                             │   │
│  │     ├─ Select: name, location, isAvailable                    │   │
│  │     └─ Return provider data                                   │   │
│  │                                                                │   │
│  │  🌍 nearbyProviders(req, res)                                 │   │
│  │     ├─ Extract lat, lng, radius from query                    │   │
│  │     ├─ MongoDB $near geospatial query                         │   │
│  │     ├─ Filter: role="provider" & isAvailable=true             │   │
│  │     ├─ Filter: onboardingStatus="approved"                    │   │
│  │     └─ Return providers array                                 │   │
│  │                                                                │   │
│  │  🔄 setAvailability(req, res)                                 │   │
│  │     ├─ Validate: onboardingStatus === "approved"              │   │
│  │     ├─ Validate: location exists (if going live)              │   │
│  │     ├─ Update: isAvailable = true/false                       │   │
│  │     └─ Return success                                         │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘

                              ⬇️ Database ⬇️

┌─────────────────────────────────────────────────────────────────────┐
│                         MONGODB DATABASE                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  🗄️  Users Collection                                               │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  {                                                             │   │
│  │    _id: ObjectId("64abc..."),                                 │   │
│  │    name: "John Doe",                                           │   │
│  │    role: "provider",                                           │   │
│  │    onboardingStatus: "approved",                               │   │
│  │    isAvailable: true,                                          │   │
│  │    location: {                                                 │   │
│  │      type: "Point",              ← GeoJSON Format              │   │
│  │      coordinates: [78.486671, 17.385044]  ← [lng, lat]         │   │
│  │    },                                                           │   │
│  │    lastLocationUpdate: ISODate("2025-10-13T10:30:00Z")        │   │
│  │  }                                                             │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  📊 Geospatial Index                                                 │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  location: "2dsphere"      ← Enables $near queries            │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  🔍 Example Query (Nearby)                                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  db.users.find({                                               │   │
│  │    role: "provider",                                           │   │
│  │    isAvailable: true,                                          │   │
│  │    location: {                                                 │   │
│  │      $near: {                                                  │   │
│  │        $geometry: {                                            │   │
│  │          type: "Point",                                        │   │
│  │          coordinates: [78.486671, 17.385044]                   │   │
│  │        },                                                       │   │
│  │        $maxDistance: 5000  ← 5km radius                        │   │
│  │      }                                                          │   │
│  │    }                                                            │   │
│  │  })                                                             │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘

                              ⬆️ GPS Data ⬆️

┌─────────────────────────────────────────────────────────────────────┐
│                         PROVIDER SIDE                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  📱 Provider Device                                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  ProviderDashboard.js                                          │   │
│  │                                                                │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  🔴 Go Live / 🟢 Go Offline Button                     │  │   │
│  │  │  ├─ Click → POST /provider/availability                │  │   │
│  │  │  └─ Activates useLiveLocation hook                     │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  │                                                                │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  🛰️  useLiveLocation Hook (Auto GPS)                   │  │   │
│  │  │                                                          │  │   │
│  │  │  useEffect(() => {                                      │  │   │
│  │  │    if (!isActive) return;                              │  │   │
│  │  │                                                          │  │   │
│  │  │    // 📡 Watch GPS position                            │  │   │
│  │  │    watchId = navigator.geolocation.watchPosition(      │  │   │
│  │  │      (pos) => sendLocation(pos.coords),                │  │   │
│  │  │      { enableHighAccuracy: true }                      │  │   │
│  │  │    );                                                   │  │   │
│  │  │                                                          │  │   │
│  │  │    // ⏰ Fallback update (every 30s)                   │  │   │
│  │  │    interval = setInterval(() => {                      │  │   │
│  │  │      getCurrentPosition((pos) => {                     │  │   │
│  │  │        sendLocation(pos.coords);                       │  │   │
│  │  │      });                                                │  │   │
│  │  │    }, 30000);                                           │  │   │
│  │  │                                                          │  │   │
│  │  │  }, [isActive]);                                        │  │   │
│  │  │                                                          │  │   │
│  │  │  📤 sendLocation():                                     │  │   │
│  │  │     POST /provider/update-location                     │  │   │
│  │  │     { lat, lng, bookingId, customerId }                │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  🛰️  Device GPS Sensor                                              │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  📡 Geolocation API                                           │   │
│  │     ├─ watchPosition() → Continuous tracking                 │   │
│  │     ├─ getCurrentPosition() → Single reading                 │   │
│  │     ├─ High accuracy enabled                                 │   │
│  │     └─ Updates: lat, lng, accuracy, timestamp                │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Sequence

### 🔄 Provider Goes Live:

```
1. Provider clicks "Go Live" button
   └─→ POST /provider/availability { isAvailable: true }

2. Backend validates:
   ├─ ✅ onboardingStatus === "approved"?
   ├─ ✅ location.coordinates exists?
   └─ ✅ Updates isAvailable = true

3. useLiveLocation hook activates:
   ├─ Starts watchPosition() GPS tracking
   ├─ Starts 30s interval fallback
   └─ Every update → POST /provider/update-location

4. Provider appears on customer maps:
   └─ GET /provider/nearby returns provider
```

---

### 📍 Customer Tracks Provider:

```
1. Customer opens /customer/track/:providerId
   └─→ GET /provider/track/:providerId

2. Backend fetches provider data:
   ├─ Query: User.findById(providerId)
   ├─ Select: name, location, isAvailable
   └─ Returns: { location: { coordinates: [lng, lat] } }

3. Frontend renders map:
   ├─ Leaflet map centered on provider
   ├─ Marker placed at coordinates
   └─ Auto-refresh every 10 seconds

4. Location updates:
   ├─ Provider moves → GPS detects → Backend updated
   ├─ 10s later → Customer polls → New coordinates
   └─ Map marker moves to new position
```

---

### 🌍 Find Nearby Providers:

```
1. Customer opens /customer/nearby
   └─→ Browser requests GPS permission

2. Get customer location:
   └─ navigator.geolocation.getCurrentPosition()
      └─ Returns: { lat: 17.385, lng: 78.486 }

3. Query nearby providers:
   └─→ GET /provider/nearby?lat=17.385&lng=78.486&radius=5000

4. MongoDB geospatial query:
   └─ $near operator finds providers within 5km
      ├─ Filters: role="provider", isAvailable=true
      ├─ Sorted by distance (closest first)
      └─ Returns array of providers

5. Render map:
   ├─ Place marker for each provider
   ├─ Add popup with provider details
   └─ Customer clicks to view/book
```

---

## File Dependencies

```
frontend/src/
├── pages/
│   ├── CustomerTrackProvider.jsx
│   │   └── Imports: TrackingMap, API
│   │       └── API calls: /provider/track/:id
│   │
│   └── NearbyProvidersMap.jsx
│       └── Imports: react-leaflet, API
│           └── API calls: /provider/nearby
│
├── hooks/
│   └── useLiveLocation.js
│       └── Imports: API
│           └── API calls: /provider/update-location
│
└── App.js
    └── Routes:
        ├── /customer/track/:providerId → CustomerTrackProvider
        └── /customer/nearby → NearbyProvidersMap

backend/src/
├── routes/
│   └── providerRoutes.js
│       └── Endpoints:
│           ├── POST /update-location
│           ├── GET /track/:id
│           ├── GET /nearby
│           └── POST /availability
│
├── controllers/
│   └── providerController.js
│       └── Functions:
│           ├── updateLocation()
│           ├── getProviderLocation()
│           ├── nearbyProviders()
│           └── setAvailability()
│
└── models/
    └── User.js
        └── Schema:
            ├── location: { type: "Point", coordinates: [lng, lat] }
            ├── isAvailable: Boolean
            ├── lastLocationUpdate: Date
            └── Index: location → "2dsphere"
```

---

## Technology Stack

```
┌─────────────────────┐
│     Frontend        │
├─────────────────────┤
│ React               │
│ Leaflet.js          │
│ react-leaflet       │
│ OpenStreetMap tiles │
│ Axios (API calls)   │
│ Custom Hooks        │
└─────────────────────┘
          ↓
┌─────────────────────┐
│      Backend        │
├─────────────────────┤
│ Node.js + Express   │
│ JWT Authentication  │
│ Role-based access   │
│ RESTful API         │
└─────────────────────┘
          ↓
┌─────────────────────┐
│     Database        │
├─────────────────────┤
│ MongoDB             │
│ GeoJSON support     │
│ 2dsphere indexing   │
│ $near queries       │
└─────────────────────┘
```

---

## Success Metrics ✅

- ✅ **3 New Pages/Components** created
- ✅ **4 API Endpoints** implemented
- ✅ **1 Custom Hook** for auto GPS
- ✅ **Geospatial Queries** working
- ✅ **Real-time Tracking** operational
- ✅ **Role-based Access** enforced
- ✅ **No External API Keys** needed (Leaflet + OSM)

---

## Next Steps 🚀

1. **WebSocket Integration**
   - Replace polling with Socket.io
   - Instant location updates
   - Bidirectional communication

2. **Animated Markers**
   - Smooth transitions
   - Pulsing effects
   - Custom icons per service

3. **Route Navigation**
   - Show path from provider to customer
   - ETA calculation
   - Turn-by-turn directions

4. **Performance Optimization**
   - Marker clustering for many providers
   - Lazy loading map tiles
   - GPS battery optimization

---

**Architecture Status:** ✅ Fully Implemented  
**All Files Present:** ✅ Verified  
**Ready for:** Production Deployment 🚀
